@{
    ViewData["Title"] = "Bienvenido";
    var usuario = ViewBag.UsuarioActual;
}

<style>
    body {
        background: linear-gradient(135deg, #2a2222 60%, #b00 100%);
        font-family: 'Segoe UI', Arial, sans-serif;
    }
    .bienvenido-container {
        background: #fff4f4;
        margin: 50px auto;
        padding: 40px 50px;
        border-radius: 16px;
        max-width: 600px;
        box-shadow: 0 8px 32px #00000044;
        text-align: center;
        position: relative;
    }
    .bienvenido-header {
        font-size: 2em;
        font-weight: bold;
        color: #b00;
        margin-bottom: 18px;
        letter-spacing: 2px;
        text-shadow: 1px 1px 0 #fff, 2px 2px 8px #b00a;
    }
    .bienvenido-user {
        position: absolute;
        top: 20px;
        right: 40px;
        background: #b00;
        color: #fff;
        padding: 8px 18px;
        border-radius: 8px;
        font-weight: bold;
        font-size: 1.1em;
        box-shadow: 0 2px 8px #b00a;
    }
    .bienvenido-content {
        margin-top: 60px;
        font-size: 1.2em;
        color: #222;
    }
</style>

<div class="bienvenido-container">
    <div class="bienvenido-user">@usuario?.Nombres</div>
    <div class="bienvenido-header">¡Bienvenido a Kongnostros!</div>
    <div class="bienvenido-content">
        <p>Ya puedes apostar, divertirte y ganar. Tu cuenta está activa.</p>
        <p>¡Mucha suerte, @usuario?.Nombres!</p>
    </div>
</div>
