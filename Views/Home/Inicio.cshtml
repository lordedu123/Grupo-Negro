
@{
    ViewData["Title"] = "Inicio";
}

<style>
    body {
        background: linear-gradient(135deg, #2a2222 60%, #b00 100%);
        font-family: 'Segoe UI', Arial, sans-serif;
    }
    .inicio-container {
        background: #fff4f4;
        margin: 50px auto;
        padding: 50px 60px 40px 60px;
        border-radius: 16px;
        max-width: 650px;
        box-shadow: 0 8px 32px #00000044;
        text-align: center;
        position: relative;
    }
    .inicio-header {
        font-size: 2.7em;
        font-weight: bold;
        color: #b00;
        margin-bottom: 8px;
        letter-spacing: 2px;
        text-shadow: 1px 1px 0 #fff, 2px 2px 8px #b00a;
    }
    .inicio-sub {
        color: #222;
        font-size: 1.3em;
        margin-bottom: 18px;
        font-weight: 500;
    }
    .inicio-motivation {
        color: #b00;
        font-size: 1.1em;
        margin-bottom: 30px;
        font-style: italic;
        font-weight: 600;
    }
    .inicio-icons {
        display: flex;
        justify-content: center;
        gap: 40px;
        margin-bottom: 30px;
    }
    .inicio-icons .icon {
        font-size: 2.5em;
        color: #b00;
        background: #fff;
        border-radius: 50%;
        box-shadow: 0 2px 8px #b00a;
        padding: 18px;
        transition: transform 0.2s;
    }
    .inicio-icons .icon:hover {
        transform: scale(1.1);
        background: #ffd6d6;
    }
    .inicio-btns {
        display: flex;
        justify-content: center;
        gap: 40px;
        margin-top: 30px;
    }
    .inicio-btns a {
        background: #ff0000;
        color: #fff;
        font-size: 1.25em;
        border: none;
        border-radius: 8px;
        padding: 18px 50px;
        text-decoration: none;
        font-weight: bold;
        box-shadow: 0 2px 8px #b00a;
        transition: background 0.2s, transform 0.2s;
    }
    .inicio-btns a:hover {
        background: #b00;
        transform: scale(1.05);
    }
    .inicio-footer {
        margin-top: 40px;
        color: #222;
        font-size: 1em;
        opacity: 0.7;
    }
</style>

<div class="inicio-container">
    <div class="inicio-header">APUESTA KONGNOSTROS</div>
    <div class="inicio-sub">¡Bienvenido a la mejor plataforma de apuestas!</div>
    <div class="inicio-motivation">Gana, diviértete y apuesta con nosotros. ¡Tu suerte comienza aquí!</div>
    <div class="inicio-icons">
        <span class="icon">🎲</span>
        <span class="icon">💰</span>
        <span class="icon">🏆</span>
        
    </div>
    <div class="inicio-btns">
    <a href="/Home/Registro">Registrarse</a>
        <a asp-controller="Home" asp-action="Login">Iniciar sesión</a>
    </div>
    <div class="inicio-footer">
        &copy; 2025 - Kongnostros. ¡Apuesta responsablemente!
    </div>
</div>
